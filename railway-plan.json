{
  "name": "snapchat-hijacker",
  "build": {
    "steps": [
      {
        "name": "install",
        "commands": ["pip install -r requirements.txt"]
      }
    ]
  },
  "deploy": {
    "start": "gunicorn --bind 0.0.0.0:$PORT app:app"
  }
}
